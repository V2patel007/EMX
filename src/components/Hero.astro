---
// No props needed for now
---

<section class="hero" id="home">
  <div class="overlay"></div>
  <div class="container">
    <div class="content">
      <h1 class="animate-fade-up">Equipment Manufacturing <span class="highlight">Xcellence</span></h1>
      <p class="tagline animate-fade-up">
        Fast. Accurate. Customizable. Serving wide range of manufacturing industries across Canada.
      </p>
      <p class="supporting-text animate-fade-up">
        Trusted by Canada's leading <strong>Conveyor system provider</strong>, 
        <strong>Control panel</strong>, and <strong>GPU manufacturers</strong>. 
        When precision matters, they trust <span class="company-name">EMX</span>.
      </p>
      <div class="buttons animate-fade-up">
        <a href="#quote" class="button button-accent">Get a Quote</a>
        <a href="#services" class="button button-secondary">View Our Capabilities</a>
      </div>
    </div>
  </div>
</section>

<style>
  /* Root cause fix: Disable fixed background for mobile */
  .hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    color: var(--color-text-light);
    background-image: url('https://images.pexels.com/photos/1145434/pexels-photo-1145434.jpeg');
    background-size: cover;
    background-position: center;
    background-attachment: fixed; /* This is the property that causes issues on iOS */
    padding: var(--space-8) var(--space-4);
    margin-top: 0;
  }
  
  /* Media query to fix iPhone issue */
  @media (max-width: 767px) {
    .hero {
      background-attachment: scroll; /* Change to scroll for mobile devices */
    }
  }

  /* Other styles remain the same for consistency and effect */
  .overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(rgba(0, 0, 0, 0.7), rgba(10, 30, 50, 0.9));
    z-index: 1;
  }

  .content {
    position: relative;
    z-index: 2;
    max-width: 900px;
    margin: 0 auto;
  }

  h1 {
    font-size: 2.25rem;
    font-weight: 800;
    margin-bottom: var(--space-4);
    text-shadow: 0 2px 4px rgba(0, 0, 0, 0.4);
  }

  .highlight {
    color: var(--color-accent-500);
  }

  .tagline {
    font-size: 1.15rem;
    color: #e0e0e0;
    margin-bottom: var(--space-4);
  }

  .supporting-text {
    font-size: 1rem;
    color: #cccccc;
    max-width: 700px;
    margin: 0 auto var(--space-8);
    line-height: 1.6;
  }

  .company-name {
    color: var(--color-accent-500);
    font-weight: 600;
  }

  .buttons {
    display: flex;
    gap: var(--space-4);
    flex-wrap: wrap;
    justify-content: center;
  }

  .animate-fade-up {
    opacity: 0;
    transform: translateY(20px);
    animation: fadeUp 0.6s ease forwards;
  }

  .animate-fade-up:nth-child(2) {
    animation-delay: 0.2s;
  }

  .animate-fade-up:nth-child(3) {
    animation-delay: 0.4s;
  }

  .animate-fade-up:nth-child(4) {
    animation-delay: 0.6s;
  }

  @keyframes fadeUp {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (min-width: 768px) {
    h1 {
      font-size: 3.25rem;
    }

    .tagline {
      font-size: 1.4rem;
      white-space: nowrap;
    }

    .supporting-text {
      font-size: 1.15rem;
    }
  }
</style>